---
phase: 17-list-view-card-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
autonomous: true
requirements:
  - CARD-01
  - CARD-02
  - CARD-03

must_haves:
  truths:
    - On wide screens (>520px), list-mode cards with a beat excerpt do NOT show the purple filled dot
    - On narrow screens (≤520px), list-mode cards with beat content DO show the purple dot
    - On wide screens, the italic beat excerpt is right-aligned within the second grid column
    - On narrow screens, the beat excerpt reverts to left-aligned (default)
    - No changes to StoryCard.tsx — CSS only
  artifacts:
    - src/index.css (two rules added to existing .story-card--list block + @media block)
---

<objective>
Add two CSS rules to hide the purple dot in wide list view and right-align the beat excerpt. Pure CSS — no component changes.

Purpose: Phase 17 cleans up the list card presentation: the dot is redundant when the excerpt is visible, and right-alignment gives the card a cleaner two-column layout feel.

Output: Two CSS additions; build passes; visual change verified manually.
</objective>

<execution_context>
@/Users/gabrielkunkel/WebstormProjects/story-x-ray/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabrielkunkel/WebstormProjects/story-x-ray/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
Current list card CSS (src/index.css ~line 387):
```css
/* ── List variant card layout ─────────────────────────────── */

.story-card--list {
  display: grid;
  grid-template-columns: minmax(160px, 280px) 1fr;
  align-items: center;
  gap: 12px 20px;
}

.story-card__beat-quote {
  font: italic 400 13px/1.5 var(--sans);
  color: var(--text);
  overflow: hidden;
  display: -webkit-box;
  -webkit-line-clamp: 3;
  -webkit-box-orient: vertical;
}

@media (max-width: 520px) {
  .story-card--list {
    grid-template-columns: 1fr;
  }
}
```

The `.story-card__filled` is the purple dot (7×7px circle).
The `.story-card__beat-quote` is in the second grid column of `.story-card--list`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dot-hide and right-align CSS</name>
  <files>src/index.css</files>
  <action>
In `src/index.css`, modify the list variant section (~line 387):

1. Add `.story-card--list .story-card__filled { display: none; }` after `.story-card--list { ... }`

2. Add `text-align: right;` to `.story-card__beat-quote`

3. Inside the existing `@media (max-width: 520px)` block, restore the dot and reset alignment:
   ```css
   .story-card--list .story-card__filled { display: block; }
   .story-card__beat-quote { text-align: left; }
   ```

Final result should look like:

```css
/* ── List variant card layout ─────────────────────────────── */

.story-card--list {
  display: grid;
  grid-template-columns: minmax(160px, 280px) 1fr;
  align-items: center;
  gap: 12px 20px;
}

.story-card--list .story-card__filled {
  display: none;
}

.story-card__beat-quote {
  font: italic 400 13px/1.5 var(--sans);
  color: var(--text);
  overflow: hidden;
  display: -webkit-box;
  -webkit-line-clamp: 3;
  -webkit-box-orient: vertical;
  text-align: right;
}

@media (max-width: 520px) {
  .story-card--list {
    grid-template-columns: 1fr;
  }

  .story-card--list .story-card__filled {
    display: block;
  }

  .story-card__beat-quote {
    text-align: left;
  }
}
```
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>`npm run build` passes. CSS added.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Manual visual verification</name>
  <files></files>
  <action>
Run `npm run dev`. Switch to list view. Use a story with beat text on several steps.

1. Wide screen (>520px):
   - Cards WITH beat text: purple dot is hidden ✓
   - Cards WITHOUT beat text: purple dot is not shown (it was never shown — no regression) ✓
   - Beat excerpt text is right-aligned within the second column ✓

2. Narrow screen (≤520px, use browser DevTools responsive mode):
   - Cards WITH beat text: purple dot IS visible ✓
   - Beat excerpt text is left-aligned (default) ✓
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Visual behavior matches spec on both wide and narrow layouts.</done>
</task>

</tasks>

<threat_model>
No trust boundaries or security concerns — CSS-only change with no data handling.
</threat_model>

<verification>
- `npm run build` passes
- In list view on wide: purple dot hidden when beat text exists
- In list view on narrow: purple dot visible when beat text exists
- Beat excerpt right-aligned on wide, left-aligned on narrow
</verification>

<success_criteria>
1. Wide list view: dot hidden on cards with beat text (CARD-01)
2. Narrow list view: dot visible on cards with beat text (CARD-02)
3. Wide list view: beat excerpt right-aligned (CARD-03)
4. npm run build passes
</success_criteria>

<output>
After completion, create `.planning/phases/17-list-view-card-polish/17-01-SUMMARY.md`.
</output>
</output>
