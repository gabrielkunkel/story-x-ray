---
phase: 15-pdf-export-overhaul
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - src/utils/export.ts
autonomous: false
requirements:
  - PDF2-05
  - PDF2-06
  - PDF2-07

must_haves:
  truths:
    - "With Scores" downloads a landscape .pdf with exactly 17 columns
    - Score columns show Conn A / Conn T / Conn Δ / Pres A / Pres T / Pres Δ / Hope A / Hope T / Hope Δ / Stab A / Stab T / Stab Δ
    - Actual score = 0 displays as em dash (—) not zero
    - Delta displays as +N / −N / — (em dash when delta = 0)
    - Target score always displays as its numeric value
    - All 16 steps appear in both export modes; empty beatText and notes cells are blank
    - Column headers repeat on every page when table spans multiple pages
    - Landscape column widths fit A4 landscape printable area without content clipping off-page
  artifacts:
    - src/utils/export.ts (exportStoryAsPDF with verified column widths)
    - .planning/phases/15-pdf-export-overhaul/15-02-SUMMARY.md
  key_links:
    - PDF2-05, PDF2-06, PDF2-07 verified against actual downloaded PDF output
---

<objective>
Verify and finalize the "With Scores" landscape PDF mode. Plan 01 implemented the full exportStoryAsPDF function including both portrait and landscape paths. This plan validates that the landscape 17-column output meets spec, adjusts column widths if visual inspection reveals overflow or clipping, and confirms all Phase 15 requirements are met.

Purpose: RESEARCH.md explicitly flagged landscape column widths as ASSUMED (not tested against actual jsPDF output). This plan exists to make those assumptions real and fix any layout issues before Phase 16 depends on this export.

Output: Verified PDF export in both modes, adjusted column widths if needed, Phase 15 complete.
</objective>

<execution_context>
@/Users/gabrielkunkel/WebstormProjects/story-x-ray/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabrielkunkel/WebstormProjects/story-x-ray/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-pdf-export-overhaul/15-CONTEXT.md
@.planning/phases/15-pdf-export-overhaul/15-01-SUMMARY.md
@.planning/phases/15-pdf-export-overhaul/15-VALIDATION.md

<interfaces>
<!-- Column width assumptions from RESEARCH.md — these are what we're verifying -->

A4 landscape printable width: 297mm - (14mm × 2 margins) = 269mm

Portrait (5 columns, 182mm usable):
  Step: 12mm | Label: 30mm | Act: 18mm | Beat Text: auto | Notes: 50mm
  Sum of fixed: 110mm → Beat Text auto ≈ 72mm

Landscape (17 columns, 269mm usable):
  Step: 12mm | Label: 28mm | Act: 18mm | Beat Text: 30mm | Notes: 25mm = 113mm base
  Score cols: 12 × 13mm = 156mm
  Total: 113 + 156 = 269mm ← exactly fills printable area (no margin for overflow)

RESEARCH.md Pitfall 2 warning: "If column widths sum to more than the printable width,
autoTable clips or wraps unpredictably. Content may render off-page."

RESEARCH.md Assumption A2: landscape column widths "must be verified by running the export"

From src/utils/export.ts (after Plan 01 — key function signature):
  export function exportStoryAsPDF(story: Story, includeScores: boolean): void
  // columnStyles for landscape mode live inside this function
  // Adjust cellWidth values here if visual inspection reveals issues
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify">
  <name>Task 1: Visually verify "With Scores" landscape PDF and multi-page behavior</name>
  <files></files>
  <action>
**Before running this task:** Confirm Plan 01 is complete (`npm run build` passes, PdfExportModal renders, "Without Scores" downloads a portrait PDF correctly).

**Generate a test story with data:**
If the current example story ("Romeo & Juliet") is loaded, it has actualScores set — use it. Otherwise, open any story and manually enter actual scores for several steps so score columns have real data (non-zero actuals) to display.

**Test "With Scores" landscape PDF:**
1. Click "↓ PDF" in the board header
2. Click "With Scores"
3. Open the downloaded .pdf file

**Check PDF2-05 — 17 columns, correct score data:**
- Count columns: Step / Label / Act / Beat Text / Notes / Conn A / Conn T / Conn Δ / Pres A / Pres T / Pres Δ / Hope A / Hope T / Hope Δ / Stab A / Stab T / Stab Δ — must be exactly 17
- PDF orientation: landscape (wider than tall)
- Score column headers readable (not wrapping to 3 lines)
- For a step with actualScore = 0 for a dimension: Actual cell shows "—" (not "0")
- For a step with actualScore > target: Delta shows "+N" (e.g., "+2")
- For a step with actualScore < target: Delta shows "−N" (e.g., "-3")
- For a step with actualScore = target: Delta shows "—"
- Target score column always shows its numeric value

**Check PDF2-06 — 16 rows, blank cells:**
- Count rows: exactly 16 (all steps)
- Steps with empty beatText: that cell is blank (not "—", not "null", not "undefined")
- Steps with empty notes: that cell is blank

**Check PDF2-07 — multi-page column headers:**
If 16 rows with beat text don't span multiple pages in landscape mode, temporarily add enough text to several steps to force a page break, then re-export. Verify column headers ("Step", "Label", etc.) appear again at the top of page 2.

**Critical layout checks:**
- No column content is clipped or cut off at the right edge of the page
- Score columns are readable at 7pt font / 6pt header
- Beat Text and Notes columns have enough width to show content (30mm and 25mm respectively)
- Table does not extend beyond the right margin

**Record your findings:**
Note any layout issues (clipping, overflow, unreadable text, headers wrapping). These feed Task 2.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
All three requirements visually confirmed in the actual PDF output:
- PDF2-05: 17 columns, landscape, score data correct (em dash for 0, delta format, target numeric)
- PDF2-06: 16 rows, blank empty cells
- PDF2-07: Column headers repeat on page 2+
  </done>
</task>

<task type="auto">
  <name>Task 2: Adjust column widths if needed + finalize VALIDATION.md</name>
  <files>src/utils/export.ts, .planning/phases/15-pdf-export-overhaul/15-VALIDATION.md</files>
  <action>
**Column width adjustment (only if Task 1 revealed layout issues):**

The landscape `columnStyles` in `exportStoryAsPDF` currently allocates exactly 269mm (the full A4 landscape printable width). jsPDF may add small internal padding that causes overflow. If Task 1 shows:

- **Content clipping at right edge:** Reduce score column widths from 13mm to 12mm each (saving 12mm total), or reduce Notes from 25mm to 20mm
- **Score column headers wrapping to 3+ lines:** Reduce font size to 5pt in `headStyles` when `includeScores: true`
- **Beat Text column too narrow to read:** Increase from 30mm to 35mm by reducing Label from 28mm to 23mm

Adjust `columnStyles` in the `includeScores ? { ... } : { ... }` block within `exportStoryAsPDF`. Verify the sum of all `cellWidth` values is ≤ 269mm after any changes.

**If Task 1 had no layout issues:** Make no changes to export.ts.

**Update `.planning/phases/15-pdf-export-overhaul/15-VALIDATION.md`:**

Change the frontmatter:
```yaml
nyquist_compliant: true
wave_0_complete: true
```

Update the task status table — mark all 15-01-xx and 15-02-xx tasks as ✅ green:
```markdown
| 15-01-01 | ... | ✅ green |
| 15-01-02 | ... | ✅ green |
| 15-01-03 | ... | ✅ green |
| 15-01-04 | ... | ✅ green |
| 15-02-01 | ... | ✅ green |
| 15-02-02 | ... | ✅ green |
| 15-02-03 | ... | ✅ green |
```

Update Wave 0 Requirements to mark the npm install complete:
```markdown
- [x] `npm install jspdf jspdf-autotable` — installed in Plan 01
```

Update Validation Sign-Off checkboxes to all checked.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
`npm run build` passes. If column widths were adjusted, the sum of all cellWidth values ≤ 269mm. VALIDATION.md has `nyquist_compliant: true` and all task rows show ✅ green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| story data → jsPDF | Score values from localStorage rendered into PDF cells |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | Information Disclosure | Score columns in PDF | accept | User is downloading their own story data to their own device — no new exposure |
| T-15-04 | Tampering | Column width math | accept | Worst outcome is unreadable PDF layout — no security impact; fixed by column width adjustment in Task 2 |
</threat_model>

<verification>
- `npm run build` passes after any column width changes
- "With Scores" PDF opens in landscape orientation with 17 readable columns
- Score data formats match spec: actual 0 → "—", delta "+N"/"-N"/"—", target always numeric
- All 16 rows present; blank cells for empty content
- Column headers repeat on page 2+ when table spans multiple pages
- VALIDATION.md updated: nyquist_compliant: true, all tasks ✅ green
</verification>

<success_criteria>
1. "With Scores" downloads a landscape PDF with exactly 17 readable columns (PDF2-05)
2. All 16 steps appear in both modes; empty cells blank, not placeholder text (PDF2-06)
3. Column headers repeat on every page when table spans multiple pages (PDF2-07)
4. npm run build passes
5. VALIDATION.md marks Phase 15 nyquist_compliant: true
</success_criteria>

<output>
After completion, create `.planning/phases/15-pdf-export-overhaul/15-02-SUMMARY.md` with:
- Visual inspection results from Task 1 (what looked good, what needed adjustment)
- Any column width changes made and why
- Confirmation of PDF2-05, PDF2-06, PDF2-07 requirements met
- Phase 15 declared complete — ready for Phase 16 (Export Dropdown)
</output>
