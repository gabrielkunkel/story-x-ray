---
phase: 27-ci-cd-documentation
plan: 02
type: execute
wave: 2
depends_on:
  - 27-01
files_modified:
  - README.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-04

must_haves:
  truths:
    - "A developer knows what .env, .env.gh-pages, and .env.example are for without reading source code"
    - "A developer knows to run npm run build:prod (not npm run build) to produce the GitHub Pages build"
    - "A developer can enable GitHub Pages by following the README steps (Settings → Pages → Source: GitHub Actions)"
    - "A developer understands that routes use the #/ prefix due to HashRouter and knows why"
  artifacts:
    - path: "README.md"
      provides: "Complete developer documentation for env files, build commands, GitHub Pages setup, and routing"
      contains: "GitHub Pages"
  key_links:
    - from: "README.md GitHub Pages section"
      to: ".env.gh-pages"
      via: "env file documentation explaining VITE_BASE_PATH=/story-x-ray/"
      pattern: "VITE_BASE_PATH"
    - from: "README.md routing note"
      to: "HashRouter"
      via: "explanation that routes use #/ prefix"
      pattern: "HashRouter"
---

<objective>
Update README.md with all deployment documentation a developer needs to understand the env file system, build commands, GitHub Pages setup procedure, and HashRouter routing convention.

Purpose: The README is the single source of truth for developers setting up or deploying the project — no tribal knowledge required.
Output: README.md with four new documentation additions covering DOC-01 through DOC-04.
</objective>

<execution_context>
@/Users/gabrielkunkel/WebstormProjects/story-x-ray/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabrielkunkel/WebstormProjects/story-x-ray/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/gabrielkunkel/WebstormProjects/story-x-ray/.planning/PROJECT.md
@/Users/gabrielkunkel/WebstormProjects/story-x-ray/.planning/ROADMAP.md
@/Users/gabrielkunkel/WebstormProjects/story-x-ray/.planning/STATE.md
@/Users/gabrielkunkel/WebstormProjects/story-x-ray/README.md

<interfaces>
<!-- Key facts for the executor — no codebase exploration needed. -->

Current README sections (DO NOT duplicate or remove these):
  1. "What it does" — feature list
  2. "Local development" — npm install + npm run dev + localhost:5173
  3. "Build" — npm run build + npm run preview (these are correct for local; build:prod is for deployment)
  4. "Enable Beehiiv email capture" — src/config/beehiiv.ts
  5. "Customize email modal" — src/config/emailModal.ts
  6. "Tech stack"
  7. "Project structure"
  8. "License" (MIT)

Exact env file contents to document:
  .env            → VITE_BASE_PATH=/
  .env.gh-pages   → VITE_BASE_PATH=/story-x-ray/
  .env.example    → documents VITE_BASE_PATH with a comment explaining both values

Correct build commands:
  npm run dev        → local development (reads .env)
  npm run build:prod → GitHub Pages build (reads .env.gh-pages via --mode gh-pages)
  npm run build      → local production build (reads .env, base /)
  npm run preview    → preview a local build

GitHub Pages setup: Settings → Pages → Source: GitHub Actions (NOT "Deploy from a branch")

HashRouter fact: Phase 26 migrated from BrowserRouter to HashRouter.
Routes are prefixed with # internally by the router. URLs look like:
  https://username.github.io/story-x-ray/#/setup
  https://username.github.io/story-x-ray/#/story/abc123
Static hosts cannot serve SPA routes — HashRouter avoids this by keeping routing client-side.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add environment files section and deployment section to README.md</name>
  <files>README.md</files>
  <read_first>
    - /Users/gabrielkunkel/WebstormProjects/story-x-ray/README.md — read the full current file before editing; insert new sections without removing any existing content
    - /Users/gabrielkunkel/WebstormProjects/story-x-ray/.env — confirm exact content: VITE_BASE_PATH=/
    - /Users/gabrielkunkel/WebstormProjects/story-x-ray/.env.gh-pages — confirm exact content: VITE_BASE_PATH=/story-x-ray/
    - /Users/gabrielkunkel/WebstormProjects/story-x-ray/.env.example — confirm exact content
  </read_first>
  <action>
Edit README.md to add the following content. Insert it after the existing "Build" section (after the `npm run preview` code block) and before the "Enable Beehiiv email capture" section. Preserve all existing content exactly.

Add this new section:

---

```markdown
## Environment files

The app uses Vite's `loadEnv` to control the asset base path at build time.

| File | Purpose |
|------|---------|
| `.env` | Local development. Sets `VITE_BASE_PATH=/` (root). Used by `npm run dev` and `npm run build`. |
| `.env.gh-pages` | GitHub Pages build. Sets `VITE_BASE_PATH=/story-x-ray/`. Used by `npm run build:prod`. |
| `.env.example` | Documentation template. Copy to `.env` and adjust for your environment. |

Both `.env` and `.env.gh-pages` are committed to the repository — they contain no secrets, only the base path value.

To deploy to a different GitHub Pages URL (e.g. `/my-fork/`), edit `.env.gh-pages`:

```
VITE_BASE_PATH=/my-fork/
```

## Deploying to GitHub Pages

A GitHub Actions workflow (`.github/workflows/deploy.yml`) automatically deploys the app on every push to `main`.

### Enable GitHub Pages (one-time setup)

1. Go to your repository on GitHub
2. Navigate to **Settings → Pages**
3. Under **Source**, select **GitHub Actions** (not "Deploy from a branch")
4. Push any commit to `main` — the workflow runs automatically

### Build commands

```bash
npm run build:prod   # GitHub Pages build — reads .env.gh-pages (VITE_BASE_PATH=/story-x-ray/)
npm run build        # Local production build — reads .env (VITE_BASE_PATH=/)
npm run preview      # Preview a local build at localhost:4173
```

Use `npm run build:prod` for deployment. Use `npm run build` to verify the local build locally.

## Routing

This app uses React Router's **HashRouter**. All routes are prefixed with `#` in the URL:

```
https://username.github.io/story-x-ray/#/
https://username.github.io/story-x-ray/#/setup
https://username.github.io/story-x-ray/#/story/abc123
```

HashRouter keeps all navigation client-side, which means static hosts (including GitHub Pages) serve a single `index.html` without needing server-side route configuration. Direct URL access and page refresh work correctly.

If you see routes without `#/` (e.g. `/setup` 404ing on refresh), check that `src/App.tsx` uses `HashRouter` from `react-router-dom`.
```

---

Placement rules:
- The "Environment files" section goes immediately after the existing "Build" section.
- The "Deploying to GitHub Pages" section goes immediately after "Environment files".
- The "Routing" section goes immediately after "Deploying to GitHub Pages".
- All three new sections appear before "Enable Beehiiv email capture".
- The existing `---` horizontal rule before "Tech stack" stays in place.
- Do NOT alter any existing heading, code block, or prose.
  </action>
  <verify>
    <automated>grep -n "Environment files" /Users/gabrielkunkel/WebstormProjects/story-x-ray/README.md && grep -n "VITE_BASE_PATH" /Users/gabrielkunkel/WebstormProjects/story-x-ray/README.md && grep -n "build:prod" /Users/gabrielkunkel/WebstormProjects/story-x-ray/README.md && grep -n "GitHub Pages" /Users/gabrielkunkel/WebstormProjects/story-x-ray/README.md && grep -n "HashRouter" /Users/gabrielkunkel/WebstormProjects/story-x-ray/README.md && grep -n "Settings.*Pages" /Users/gabrielkunkel/WebstormProjects/story-x-ray/README.md && grep -n "GitHub Actions" /Users/gabrielkunkel/WebstormProjects/story-x-ray/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Environment files" README.md` returns a match (DOC-01 section heading present)
    - `grep "VITE_BASE_PATH=/" README.md` returns a match (local env value documented)
    - `grep "VITE_BASE_PATH=/story-x-ray/" README.md` returns a match (gh-pages env value documented)
    - `grep "\.env\.gh-pages" README.md` returns a match (file name documented)
    - `grep "\.env\.example" README.md` returns a match (file name documented)
    - `grep "build:prod" README.md` returns a match (DOC-02 build command documented)
    - `grep "Settings.*Pages\|Pages.*Settings" README.md` returns a match (DOC-03 setup step present)
    - `grep "GitHub Actions" README.md` returns at least 2 matches (workflow reference + Pages source setting)
    - `grep "HashRouter" README.md` returns a match (DOC-04 routing note present)
    - `grep "#/" README.md` returns a match (hash prefix routing convention documented)
    - `grep "Local development" README.md` returns a match (existing section not removed)
    - `grep "Enable Beehiiv" README.md` returns a match (existing section not removed)
    - `grep "Tech stack" README.md` returns a match (existing section not removed)
    - `grep "MIT" README.md` returns a match (license not removed)
  </acceptance_criteria>
  <done>README.md contains all four documentation requirements (DOC-01 through DOC-04) with no existing content removed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README reader → repository | README is public documentation; no secrets should be present |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-04 | Information Disclosure | README.md | accept | README documents env var names and values — these contain only base path config (no secrets); safe to commit and publish |
</threat_model>

<verification>
After README.md is updated:

1. `grep -c "VITE_BASE_PATH" README.md` — should return 3 or more (one per env file table row + code example)
2. `grep -c "GitHub Pages" README.md` — should return 3 or more
3. `grep "HashRouter" README.md` — should return the routing section heading or explanation
4. Read the full README from "Build" through "Enable Beehiiv email capture" to confirm correct insertion order and no duplicate headings
</verification>

<success_criteria>
- README.md has a new "Environment files" section explaining .env, .env.gh-pages, and .env.example (DOC-01)
- README.md documents npm run build:prod alongside the existing build commands (DOC-02)
- README.md includes GitHub Pages one-time setup steps: Settings → Pages → Source: GitHub Actions (DOC-03)
- README.md explains HashRouter and the #/ routing convention (DOC-04)
- All existing README sections remain intact and unmodified
</success_criteria>

<output>
After completion, create `.planning/phases/27-ci-cd-documentation/27-02-SUMMARY.md` following the summary template.
</output>
