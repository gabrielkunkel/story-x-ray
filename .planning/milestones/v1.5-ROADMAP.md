# Milestone Archive: v1.5 Stories Browser & Email Capture

**Shipped:** 2026-04-06
**Phases:** 19–21 (3 phases)
**Plans:** 2 (Phase 21 — Phases 19 & 20 executed directly)
**Commits:** d597ace → ce2383a
**Files changed:** 22 files, +2073/-43 lines

## Goal

Give users access to all their saved stories from the start screen, fix the PWA console warning, and make the email capture modal more compelling with a smarter trigger and optional marketing image.

## Phases

### Phase 19: PWA Console Warning Fix
**Goal:** Remove the "Banner not shown" console warning by dropping `e.preventDefault()` from the `beforeinstallprompt` handler.
**Requirements:** PWA-03

**Success Criteria (all met):**
1. No "Banner not shown: beforeinstallpromptevent.preventDefault() called" message in Chrome console ✅
2. The install callout still appears correctly when the app is installable ✅

**Plans:** Executed directly (no GSD plan artifacts)
- [x] Remove `e.preventDefault()` from `usePWAInstall.ts` — commit `d597ace`

### Phase 20: Story Project Browser
**Goal:** Users can see and open all their saved stories from the start screen.
**Requirements:** STORIES-01, STORIES-02, STORIES-03, STORIES-04, STORIES-05

**Success Criteria (all met):**
1. Start screen lists all saved stories ordered by most recently updated ✅
2. Each story shows title and last-updated date ✅
3. Clicking a story navigates to its workspace ✅
4. Each story has a delete action with a confirmation step ✅
5. No stories → list is hidden, start screen unchanged ✅

**Plans:** Executed directly (no GSD plan artifacts)
- [x] Story list UI + delete with confirmation on StartPage — commit `07e1618`

### Phase 21: Email Capture Redesign
**Goal:** Trigger the email modal after any 4 beats are filled (not just Act I), and add optional marketing image + configurable copy to the modal.
**Requirements:** CAPTURE-01, CAPTURE-02, CAPTURE-03, CAPTURE-04, CAPTURE-05

**Success Criteria (all met):**
1. Email modal triggers when any 4 beats (beatText non-empty) exist across the whole story ✅
2. Trigger is per-session: fires once per session if email not yet submitted ✅
3. Modal optionally renders a marketing image above the headline when `MODAL_IMAGE_SRC` is set ✅
4. Headline and body copy defined in one config block in `EmailCaptureModal.tsx` ✅
5. README.md explains how to update the image and copy ✅

**Plans:**
- [x] 21-01 — Trigger logic change + configurable copy (commits `7cfe05f`, `60d2a26`)
- [x] 21-02 — Marketing image support in modal (commits `d3c283d`, `c2fe750`)

## Key Accomplishments

1. **PWA console warning eliminated** — Removed unnecessary `e.preventDefault()` from `beforeinstallprompt`; callout continues to work correctly
2. **Story browser on StartPage** — All saved stories listed by recency; click to open, delete with confirmation; hidden when no stories exist
3. **Smarter email trigger** — 4-beat threshold across all 16 steps replaces Act I–only gate; writers who fill beats in any act now see the modal sooner
4. **Configurable modal copy** — Single `COPY` + `MODAL_IMAGE_SRC` config block at top of `EmailCaptureModal.tsx`; easy to update without touching component logic
5. **Marketing image support** — Conditional `<img>` render above headline; `alt=""` + `aria-hidden="true"` for decorative accessibility; no broken box when empty
6. **Developer documentation** — README "Customize email modal" section documents image dimensions, file path, and copy editing with step-by-step instructions

## Files Changed

| File | Change |
|------|--------|
| `src/hooks/usePWAInstall.ts` | Removed `e.preventDefault()` from beforeinstallprompt |
| `src/pages/StartPage.tsx` | Added story list with open/delete/confirmation UI |
| `src/pages/StoryWorkspacePage.tsx` | Updated beat trigger logic (Act I → 4-beat threshold) |
| `src/components/EmailCaptureModal.tsx` | Added MODAL_IMAGE_SRC constant, config block, conditional image JSX |
| `src/index.css` | Added `.capture-modal__image` CSS rule |
| `README.md` | Added "Customize email modal" documentation section |

## Tech Debt Accepted

- Phases 19 & 20 were executed outside GSD executor (committed directly). No SUMMARY.md, VERIFICATION.md, or VALIDATION.md exists for these phases. Code is correct and all requirements verified by integration checker. Use GSD executor for future phases.
- Phase 21 `nyquist_compliant: false` — manual-only validation; no automated test framework in project.
